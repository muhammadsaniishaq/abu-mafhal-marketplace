{
    "buildCommand": "npm run build && cd mobile && npm install && export EXPO_PUBLIC_BASE_PATH=/mobile && npx expo export --platform web && cd .. && mkdir -p dist/mobile && cp -R mobile/dist/* dist/mobile/",
    "outputDirectory": "dist",
    "framework": "vite"
}