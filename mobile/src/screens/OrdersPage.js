import React from 'react';
import { View, Text } from 'react-native';

export const OrdersPage = ({ onBack, user, onNavigate }) => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Orders Page (Restored)</Text>
        </View>
    );
};
